// Generated by tilelang.hexagon.emitter: generic mixed pool/HMX shell v1.
// T.parallel phases are synchronous worker-pool jobs; serial/T.gemm phases run on the caller thread.

// tl.hexagon_prof profile slots (int32 qtimer ticks, accumulated like gsp_prof):
//   prof[0]: pre-loop/weight-staging aggregate when emitted (legacy generic slot)
//   prof[1]: main-thread staging copies (rm/f32 -> AH/WH) aggregate
//   prof[2]: main-thread HMX matmul + accumulator read aggregate
//   prof[3]: main-thread accumulator readback/unpermute/writeback aggregate
//   prof[4]: whole entry wall time
//   prof[5..16]: worker-pool calls in source order (one slot per synchronous T.parallel phase)
#include <stdint.h>
#include <stddef.h>
#include <string.h>
#include "attnops.h"
#include "HAP_perf.h"
#include "hexagon_rt.h"

typedef hrt_f16 f16;
typedef hrt_f32 f32;

#define TL_GENERIC_VTCM_BYTES ((size_t)6606848)
#define TL_ACC ((size_t)3014656)

typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int p;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool0_ctx_t;

static void attnops_tl_ffn_std_pool0_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool0_ctx_t *ctx = (const attnops_tl_ffn_std_pool0_ctx_t *)opaque;
    const f16 *Wg = ctx->Wg;
    const f16 *Wu = ctx->Wu;
    const int p = ctx->p;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Wg; (void)Wu; (void)p; (void)abl;
    const int w_job = job;
    if ((w_job < 8)) {
        if (!(abl & 2)) {
            hrt_copy_wh_block(V + 327680 + (((size_t)((w_job * 32)) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES, (const uint8_t *)(Wg) + (((size_t)(((p * 256) + (w_job * 32))) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES, 32, 2560);
        }
    } else {
        if (!(abl & 2)) {
            hrt_copy_wh_block(V + 327680 + (((size_t)((w_job * 32)) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES, (const uint8_t *)(Wu) + (((size_t)((((p * 256) + (w_job * 32)) - 256)) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES, 32, 2560);
        }
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int p;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool1_ctx_t;

static void attnops_tl_ffn_std_pool1_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool1_ctx_t *ctx = (const attnops_tl_ffn_std_pool1_ctx_t *)opaque;
    const f16 *X = ctx->X;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)X; (void)abl;
    const int xjob = job;
    if (!(abl & 2)) {
        hrt_mask_init();
        for (int tl_gm_r = 0; tl_gm_r < 1; tl_gm_r++)
            hrt_stage_act_hvx_direct_strided((const uint8_t *)(X + (size_t)((size_t)((xjob * 320))) + (size_t)tl_gm_r * 32 * 2560),
                V + 0 + (((size_t)(0) / 32) * 80 + ((size_t)((xjob * 320)) / 32)) * HRT_TILE_BYTES + (size_t)tl_gm_r * 10 * HRT_TILE_BYTES, 2560, 320, 10, HRT_TILE_BYTES, 32);
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int p;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool2_ctx_t;

static void attnops_tl_ffn_std_pool2_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool2_ctx_t *ctx = (const attnops_tl_ffn_std_pool2_ctx_t *)opaque;
    const f16 *X = ctx->X;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)X; (void)abl;
    const int xjob = job;
    if (!(abl & 2)) {
        hrt_mask_init();
        for (int tl_gm_r = 0; tl_gm_r < 1; tl_gm_r++)
            hrt_stage_act_hvx_direct_strided((const uint8_t *)(X + (size_t)((size_t)(32) * (2560) + (size_t)((xjob * 320))) + (size_t)tl_gm_r * 32 * 2560),
                V + 0 + (((size_t)(32) / 32) * 80 + ((size_t)((xjob * 320)) / 32)) * HRT_TILE_BYTES + (size_t)tl_gm_r * 10 * HRT_TILE_BYTES, 2560, 320, 10, HRT_TILE_BYTES, 32);
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int p;
    int mb;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool3_ctx_t;

static void attnops_tl_ffn_std_pool3_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool3_ctx_t *ctx = (const attnops_tl_ffn_std_pool3_ctx_t *)opaque;
    const f16 *X = ctx->X;
    f16 *Slab = ctx->Slab;
    const int p = ctx->p;
    const int mb = ctx->mb;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)X; (void)Slab; (void)p; (void)mb; (void)abl;
    if (job < 8) {
        const int xjob = job;
        if (!(abl & 2)) {
            hrt_mask_init();
            for (int tl_gm_r = 0; tl_gm_r < 1; tl_gm_r++)
                hrt_stage_act_hvx_direct_strided((const uint8_t *)(X + (size_t)((size_t)(((mb * 32) + 64)) * (2560) + (size_t)((xjob * 320))) + (size_t)tl_gm_r * 32 * 2560),
                    V + 0 + (((size_t)(((mb % 2) * 32)) / 32) * 80 + ((size_t)((xjob * 320)) / 32)) * HRT_TILE_BYTES + (size_t)tl_gm_r * 10 * HRT_TILE_BYTES, 2560, 320, 10, HRT_TILE_BYTES, 32);
        }
    } else {
        const int r = job - 8;
        // T.vectorized(256) -> 128B HVX vector loop
        for (int c = 0; c < 256; c += 64) {
            size_t hvx_idx2 = (size_t)((((mb * 32) + r)) * 9216 + (((p * 256) + c)));
            if (hvx_idx2 + 64 <= (size_t)(2048 * 9216)) {
                HVX_Vector hv0 = (*(const HVX_Vector *)(((f16 *)(V + 2949120)) + (size_t)(((mb % 2)) * 32 * 256 + (r) * 256 + (c))));
                HVX_Vector hv1, hv2;
                hrt_h2f_vec_pair(hv0, &hv1, &hv2);
                HVX_Vector hv3 = hrt_vsplat_f32_bits(0x3F800000);
                HVX_Vector hv4 = (*(const HVX_Vector *)(((f16 *)(V + 2949120)) + (size_t)(((mb % 2)) * 32 * 256 + (r) * 256 + (c))));
                HVX_Vector hv5, hv6;
                hrt_h2f_vec_pair(hv4, &hv5, &hv6);
                HVX_Vector hv7 = hrt_vsplat_f32_bits(0xBF800000);
                HVX_Vector hv8 = Q6_Vsf_vmpy_VsfVsf(hv5, hv7);
                HVX_Vector hv9 = Q6_Vsf_vmpy_VsfVsf(hv6, hv7);
                HVX_Vector hv10 = hrt_exp_fp32_vec(hv8);
                HVX_Vector hv11 = hrt_exp_fp32_vec(hv9);
                HVX_Vector hv12 = Q6_Vsf_vadd_VsfVsf(hv3, hv10);
                HVX_Vector hv13 = Q6_Vsf_vadd_VsfVsf(hv3, hv11);
                HVX_Vector hv14 = hrt_vsf_div_approx(hv1, hv12);
                HVX_Vector hv15 = hrt_vsf_div_approx(hv2, hv13);
                HVX_Vector hv16 = (*(const HVX_Vector *)(((f16 *)(V + 2981888)) + (size_t)(((mb % 2)) * 32 * 256 + (r) * 256 + (c))));
                HVX_Vector hv17, hv18;
                hrt_h2f_vec_pair(hv16, &hv17, &hv18);
                HVX_Vector hv19 = Q6_Vsf_vmpy_VsfVsf(hv14, hv17);
                HVX_Vector hv20 = Q6_Vsf_vmpy_VsfVsf(hv15, hv18);
                HVX_Vector hv21 = hrt_f2h_vec_pair(hv19, hv20);
                *(HVX_Vector *)(Slab + hvx_idx2) = hv21;
            }
        }
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int p;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool4_ctx_t;

static void attnops_tl_ffn_std_pool4_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool4_ctx_t *ctx = (const attnops_tl_ffn_std_pool4_ctx_t *)opaque;
    f16 *Slab = ctx->Slab;
    const int p = ctx->p;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Slab; (void)p;
    const int r = job;
    // T.vectorized(256) -> 128B HVX vector loop
    for (int c = 0; c < 256; c += 64) {
        size_t hvx_idx7 = (size_t)(((r + 960)) * 9216 + (((p * 256) + c)));
        if (hvx_idx7 + 64 <= (size_t)(2048 * 9216)) {
            HVX_Vector hv22 = (*(const HVX_Vector *)(((f16 *)(V + 2949120)) + (size_t)((0) * 32 * 256 + (r) * 256 + (c))));
            HVX_Vector hv23, hv24;
            hrt_h2f_vec_pair(hv22, &hv23, &hv24);
            HVX_Vector hv25 = hrt_vsplat_f32_bits(0x3F800000);
            HVX_Vector hv26 = (*(const HVX_Vector *)(((f16 *)(V + 2949120)) + (size_t)((0) * 32 * 256 + (r) * 256 + (c))));
            HVX_Vector hv27, hv28;
            hrt_h2f_vec_pair(hv26, &hv27, &hv28);
            HVX_Vector hv29 = hrt_vsplat_f32_bits(0xBF800000);
            HVX_Vector hv30 = Q6_Vsf_vmpy_VsfVsf(hv27, hv29);
            HVX_Vector hv31 = Q6_Vsf_vmpy_VsfVsf(hv28, hv29);
            HVX_Vector hv32 = hrt_exp_fp32_vec(hv30);
            HVX_Vector hv33 = hrt_exp_fp32_vec(hv31);
            HVX_Vector hv34 = Q6_Vsf_vadd_VsfVsf(hv25, hv32);
            HVX_Vector hv35 = Q6_Vsf_vadd_VsfVsf(hv25, hv33);
            HVX_Vector hv36 = hrt_vsf_div_approx(hv23, hv34);
            HVX_Vector hv37 = hrt_vsf_div_approx(hv24, hv35);
            HVX_Vector hv38 = (*(const HVX_Vector *)(((f16 *)(V + 2981888)) + (size_t)((0) * 32 * 256 + (r) * 256 + (c))));
            HVX_Vector hv39, hv40;
            hrt_h2f_vec_pair(hv38, &hv39, &hv40);
            HVX_Vector hv41 = Q6_Vsf_vmpy_VsfVsf(hv36, hv39);
            HVX_Vector hv42 = Q6_Vsf_vmpy_VsfVsf(hv37, hv40);
            HVX_Vector hv43 = hrt_f2h_vec_pair(hv41, hv42);
            *(HVX_Vector *)(Slab + hvx_idx7) = hv43;
        }
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int p;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool5_ctx_t;

static void attnops_tl_ffn_std_pool5_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool5_ctx_t *ctx = (const attnops_tl_ffn_std_pool5_ctx_t *)opaque;
    f16 *Slab = ctx->Slab;
    const int p = ctx->p;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Slab; (void)p;
    const int r = job;
    // T.vectorized(256) -> 128B HVX vector loop
    for (int c = 0; c < 256; c += 64) {
        size_t hvx_idx8 = (size_t)(((r + 992)) * 9216 + (((p * 256) + c)));
        if (hvx_idx8 + 64 <= (size_t)(2048 * 9216)) {
            HVX_Vector hv44 = (*(const HVX_Vector *)(((f16 *)(V + 2949120)) + (size_t)((1) * 32 * 256 + (r) * 256 + (c))));
            HVX_Vector hv45, hv46;
            hrt_h2f_vec_pair(hv44, &hv45, &hv46);
            HVX_Vector hv47 = hrt_vsplat_f32_bits(0x3F800000);
            HVX_Vector hv48 = (*(const HVX_Vector *)(((f16 *)(V + 2949120)) + (size_t)((1) * 32 * 256 + (r) * 256 + (c))));
            HVX_Vector hv49, hv50;
            hrt_h2f_vec_pair(hv48, &hv49, &hv50);
            HVX_Vector hv51 = hrt_vsplat_f32_bits(0xBF800000);
            HVX_Vector hv52 = Q6_Vsf_vmpy_VsfVsf(hv49, hv51);
            HVX_Vector hv53 = Q6_Vsf_vmpy_VsfVsf(hv50, hv51);
            HVX_Vector hv54 = hrt_exp_fp32_vec(hv52);
            HVX_Vector hv55 = hrt_exp_fp32_vec(hv53);
            HVX_Vector hv56 = Q6_Vsf_vadd_VsfVsf(hv47, hv54);
            HVX_Vector hv57 = Q6_Vsf_vadd_VsfVsf(hv47, hv55);
            HVX_Vector hv58 = hrt_vsf_div_approx(hv45, hv56);
            HVX_Vector hv59 = hrt_vsf_div_approx(hv46, hv57);
            HVX_Vector hv60 = (*(const HVX_Vector *)(((f16 *)(V + 2981888)) + (size_t)((1) * 32 * 256 + (r) * 256 + (c))));
            HVX_Vector hv61, hv62;
            hrt_h2f_vec_pair(hv60, &hv61, &hv62);
            HVX_Vector hv63 = Q6_Vsf_vmpy_VsfVsf(hv58, hv61);
            HVX_Vector hv64 = Q6_Vsf_vmpy_VsfVsf(hv59, hv62);
            HVX_Vector hv65 = hrt_f2h_vec_pair(hv63, hv64);
            *(HVX_Vector *)(Slab + hvx_idx8) = hv65;
        }
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int kp;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool6_ctx_t;

static void attnops_tl_ffn_std_pool6_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool6_ctx_t *ctx = (const attnops_tl_ffn_std_pool6_ctx_t *)opaque;
    const f16 *Wd = ctx->Wd;
    const int kp = ctx->kp;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Wd; (void)kp; (void)abl;
    const int wd_job = job;
    if (!(abl & 2)) {
        hrt_copy_wh_block(V + 4227072 + (((size_t)((wd_job * 32)) / 32) * 288 + ((size_t)(0) / 32)) * HRT_TILE_BYTES, (const uint8_t *)(Wd) + (((size_t)(((kp * 128) + (wd_job * 32))) / 32) * 288 + ((size_t)(0) / 32)) * HRT_TILE_BYTES, 32, 9216);
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int kp;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool7_ctx_t;

static void attnops_tl_ffn_std_pool7_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool7_ctx_t *ctx = (const attnops_tl_ffn_std_pool7_ctx_t *)opaque;
    f16 *Slab = ctx->Slab;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Slab; (void)abl;
    const int hjob = job;
    if (!(abl & 2)) {
        hrt_mask_init();
        for (int tl_gm_r = 0; tl_gm_r < 1; tl_gm_r++)
            hrt_stage_act_hvx_direct_strided((const uint8_t *)(Slab + (size_t)((size_t)((hjob * 256))) + (size_t)tl_gm_r * 32 * 9216),
                V + 3047424 + (((size_t)(0) / 32) * 288 + ((size_t)((hjob * 256)) / 32)) * HRT_TILE_BYTES + (size_t)tl_gm_r * 8 * HRT_TILE_BYTES, 9216, 256, 8, HRT_TILE_BYTES, 32);
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int kp;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool8_ctx_t;

static void attnops_tl_ffn_std_pool8_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool8_ctx_t *ctx = (const attnops_tl_ffn_std_pool8_ctx_t *)opaque;
    f16 *Slab = ctx->Slab;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Slab; (void)abl;
    const int hjob = job;
    if (!(abl & 2)) {
        hrt_mask_init();
        for (int tl_gm_r = 0; tl_gm_r < 1; tl_gm_r++)
            hrt_stage_act_hvx_direct_strided((const uint8_t *)(Slab + (size_t)((size_t)(32) * (9216) + (size_t)((hjob * 256))) + (size_t)tl_gm_r * 32 * 9216),
                V + 3047424 + (((size_t)(32) / 32) * 288 + ((size_t)((hjob * 256)) / 32)) * HRT_TILE_BYTES + (size_t)tl_gm_r * 8 * HRT_TILE_BYTES, 9216, 256, 8, HRT_TILE_BYTES, 32);
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int kp;
    int rb2;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool9_ctx_t;

static void attnops_tl_ffn_std_pool9_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool9_ctx_t *ctx = (const attnops_tl_ffn_std_pool9_ctx_t *)opaque;
    f16 *Slab = ctx->Slab;
    const int kp = ctx->kp;
    const int rb2 = ctx->rb2;
    const int abl = ctx->abl;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Slab; (void)kp; (void)rb2; (void)abl;
    if (job < 36) {
        const int hjob = job;
        if (!(abl & 2)) {
            hrt_mask_init();
            for (int tl_gm_r = 0; tl_gm_r < 1; tl_gm_r++)
                hrt_stage_act_hvx_direct_strided((const uint8_t *)(Slab + (size_t)((size_t)(((rb2 * 32) + 64)) * (9216) + (size_t)((hjob * 256))) + (size_t)tl_gm_r * 32 * 9216),
                    V + 3047424 + (((size_t)(((rb2 % 2) * 32)) / 32) * 288 + ((size_t)((hjob * 256)) / 32)) * HRT_TILE_BYTES + (size_t)tl_gm_r * 8 * HRT_TILE_BYTES, 9216, 256, 8, HRT_TILE_BYTES, 32);
        }
    } else {
        const int ry = job - 36;
        // T.vectorized(128) -> 128B HVX vector loop
        for (int cy = 0; cy < 128; cy += 64) {
            size_t hvx_idx10 = (size_t)(((((rb2 * 32) + ry) + 1024)) * 9216 + (((kp * 128) + cy)));
            if (hvx_idx10 + 64 <= (size_t)(2048 * 9216)) {
                HVX_Vector hv66 = (*(const HVX_Vector *)(((f16 *)(V + 6586368)) + (size_t)(((rb2 % 2)) * 32 * 128 + (ry) * 128 + (cy))));
                *(HVX_Vector *)(Slab + hvx_idx10) = hv66;
            }
        }
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int kp;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool10_ctx_t;

static void attnops_tl_ffn_std_pool10_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool10_ctx_t *ctx = (const attnops_tl_ffn_std_pool10_ctx_t *)opaque;
    f16 *Slab = ctx->Slab;
    const int kp = ctx->kp;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Slab; (void)kp;
    const int ry = job;
    // T.vectorized(128) -> 128B HVX vector loop
    for (int cy = 0; cy < 128; cy += 64) {
        size_t hvx_idx13 = (size_t)(((ry + 1984)) * 9216 + (((kp * 128) + cy)));
        if (hvx_idx13 + 64 <= (size_t)(2048 * 9216)) {
            HVX_Vector hv67 = (*(const HVX_Vector *)(((f16 *)(V + 6586368)) + (size_t)((0) * 32 * 128 + (ry) * 128 + (cy))));
            *(HVX_Vector *)(Slab + hvx_idx13) = hv67;
        }
    }
}


typedef struct {
    const f16 *X;
    const f16 *Wg;
    const f16 *Wu;
    const f16 *Wd;
    f16 *Slab;
    int kp;
    int abl;
    int32_t *prof;
} attnops_tl_ffn_std_pool11_ctx_t;

static void attnops_tl_ffn_std_pool11_worker(int job, void *opaque) {
    const attnops_tl_ffn_std_pool11_ctx_t *ctx = (const attnops_tl_ffn_std_pool11_ctx_t *)opaque;
    f16 *Slab = ctx->Slab;
    const int kp = ctx->kp;
    uint8_t *V = HRT_VTCM_BASE();
    uint64_t t0; (void)V; (void)t0; (void)Slab; (void)kp;
    const int ry = job;
    // T.vectorized(128) -> 128B HVX vector loop
    for (int cy = 0; cy < 128; cy += 64) {
        size_t hvx_idx14 = (size_t)(((ry + 2016)) * 9216 + (((kp * 128) + cy)));
        if (hvx_idx14 + 64 <= (size_t)(2048 * 9216)) {
            HVX_Vector hv68 = (*(const HVX_Vector *)(((f16 *)(V + 6586368)) + (size_t)((1) * 32 * 128 + (ry) * 128 + (cy))));
            *(HVX_Vector *)(Slab + hvx_idx14) = hv68;
        }
    }
}


int attnops_tl_ffn_std(remote_handle64 h, unsigned char *slab, int slabLen, int abl) {
    (void)h;
    if (!HRT_VTCM_READY()) return -3;
    if (TL_GENERIC_VTCM_BYTES > HRT_VTCM_SIZE()) return -4;
    size_t off = 0;
    const f16 *X = (const f16 *)(slab + off);
    off = HRT_ALIGN128(off + ((size_t)1024 * (size_t)2560) * 2);
    const f16 *Wg = (const f16 *)(slab + off);
    off = HRT_ALIGN128(off + ((size_t)9216 * (size_t)2560) * 2);
    const f16 *Wu = (const f16 *)(slab + off);
    off = HRT_ALIGN128(off + ((size_t)9216 * (size_t)2560) * 2);
    const f16 *Wd = (const f16 *)(slab + off);
    off = HRT_ALIGN128(off + ((size_t)2560 * (size_t)9216) * 2);
    f16 *Slab = (f16 *)(slab + off);
    off = HRT_ALIGN128(off + ((size_t)2048 * (size_t)9216) * 2);
    size_t prof_off = off;
    if ((size_t)slabLen < prof_off + 68) return -1;
    HRT_PROF_DECL(prof, slab, prof_off);
    HRT_PROF_CLEAR(prof, 68);
    uint8_t *V = HRT_VTCM_BASE();
    const size_t total = (size_t)-1;
    (void)V; (void)total;
    uint64_t t0, tl_prof_phase_t0, tt = HAP_perf_get_qtimer_count();
    // TIR launch_thread(blockIdx.x, extent=1)
    for (int bx = 0; bx < 1; bx++) {
        // TIR threadIdx.x extent=1 is lowered into hrt_* worker-pool recipes
        // TIR threadIdx.y extent=1 is lowered into hrt_* worker-pool recipes
        // TIR threadIdx.z extent=1 is lowered into hrt_* worker-pool recipes
        for (int p = 0; p < 36; p++) {
            // T.Parallel(16) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool0_ctx_t ctx0 = { X, Wg, Wu, Wd, Slab, p, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool0_worker, &ctx0, 16);
            prof[5] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // T.Parallel(8) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool1_ctx_t ctx1 = { X, Wg, Wu, Wd, Slab, p, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool1_worker, &ctx1, 8);
            prof[6] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // async_commit_group consumed by W-lane pool start
            // T.Parallel(8) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool2_ctx_t ctx2 = { X, Wg, Wu, Wd, Slab, p, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool2_worker, &ctx2, 8);
            prof[7] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // async_commit_group consumed by W-lane pool start
            attnops_pool_wait_le_w(1);
            t0 = HAP_perf_get_qtimer_count();
            for (int gm0_r = 0; gm0_r < 1; gm0_r++) {
                for (int gm0_c = 0; gm0_c < 8; gm0_c++) {
                    if (!(abl & 4)) {
                        int e = hrt_acc_clear_f16();
                        if (e) return e;
                        for (int gm0_kb = 0; gm0_kb < 80; gm0_kb++) {
                            e = hrt_hmx_mm_f16(V, 0 + ((size_t)gm0_r * 80 + gm0_kb) * HRT_TILE_BYTES,
                                327680 + ((size_t)gm0_c * 80 + gm0_kb) * HRT_TILE_BYTES);
                            if (e) return e;
                        }
                        if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                    }
                    prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                    if (!(abl & 8)) {
                        hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 2949120), 256, gm0_r, gm0_c);
                    }
                    prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                }
            }
            t0 = HAP_perf_get_qtimer_count();
            for (int gm1_r = 0; gm1_r < 1; gm1_r++) {
                for (int gm1_c = 0; gm1_c < 8; gm1_c++) {
                    if (!(abl & 4)) {
                        int e = hrt_acc_clear_f16();
                        if (e) return e;
                        for (int gm1_kb = 0; gm1_kb < 80; gm1_kb++) {
                            e = hrt_hmx_mm_f16(V, 0 + ((size_t)gm1_r * 80 + gm1_kb) * HRT_TILE_BYTES,
                                327680 + (((size_t)(256) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm1_c * 80 + gm1_kb) * HRT_TILE_BYTES);
                            if (e) return e;
                        }
                        if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                    }
                    prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                    if (!(abl & 8)) {
                        hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 2981888), 256, gm1_r, gm1_c);
                    }
                    prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                }
            }
            // T.Pipelined steady loop: first worker-pool phase is async; join before next pool phase or loop back-edge
            for (int mb = 0; mb < 30; mb++) {
                // T.Parallel(8)+T.Parallel(32) -> fused Hexagon worker-pool phase (async start)
                attnops_tl_ffn_std_pool3_ctx_t ctx3 = { X, Wg, Wu, Wd, Slab, p, mb, abl, prof };
                tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
                attnops_pool_start_ctx(attnops_tl_ffn_std_pool3_worker, &ctx3, 40);
                // async_commit_group consumed by W-lane pool start
                attnops_pool_wait_le_w(1);
                t0 = HAP_perf_get_qtimer_count();
                for (int gm3_r = 0; gm3_r < 1; gm3_r++) {
                    for (int gm3_c = 0; gm3_c < 8; gm3_c++) {
                        if (!(abl & 4)) {
                            int e = hrt_acc_clear_f16();
                            if (e) return e;
                            for (int gm3_kb = 0; gm3_kb < 80; gm3_kb++) {
                                e = hrt_hmx_mm_f16(V, 0 + (((size_t)((((mb + 1) % 2) * 32)) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm3_r * 80 + gm3_kb) * HRT_TILE_BYTES,
                                    327680 + ((size_t)gm3_c * 80 + gm3_kb) * HRT_TILE_BYTES);
                                if (e) return e;
                            }
                            if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                        }
                        prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                        t0 = HAP_perf_get_qtimer_count();
                        if (!(abl & 8)) {
                            hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 2949120) + (size_t)((size_t)(((mb + 1) % 2)) * (32 * 256)), 256, gm3_r, gm3_c);
                        }
                        prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                        t0 = HAP_perf_get_qtimer_count();
                    }
                }
                t0 = HAP_perf_get_qtimer_count();
                for (int gm4_r = 0; gm4_r < 1; gm4_r++) {
                    for (int gm4_c = 0; gm4_c < 8; gm4_c++) {
                        if (!(abl & 4)) {
                            int e = hrt_acc_clear_f16();
                            if (e) return e;
                            for (int gm4_kb = 0; gm4_kb < 80; gm4_kb++) {
                                e = hrt_hmx_mm_f16(V, 0 + (((size_t)((((mb + 1) % 2) * 32)) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm4_r * 80 + gm4_kb) * HRT_TILE_BYTES,
                                    327680 + (((size_t)(256) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm4_c * 80 + gm4_kb) * HRT_TILE_BYTES);
                                if (e) return e;
                            }
                            if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                        }
                        prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                        t0 = HAP_perf_get_qtimer_count();
                        if (!(abl & 8)) {
                            hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 2981888) + (size_t)((size_t)(((mb + 1) % 2)) * (32 * 256)), 256, gm4_r, gm4_c);
                        }
                        prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                        t0 = HAP_perf_get_qtimer_count();
                    }
                }
                attnops_pool_join();
                prof[8] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            }
            attnops_pool_wait_le_w(0);
            t0 = HAP_perf_get_qtimer_count();
            for (int gm5_r = 0; gm5_r < 1; gm5_r++) {
                for (int gm5_c = 0; gm5_c < 8; gm5_c++) {
                    if (!(abl & 4)) {
                        int e = hrt_acc_clear_f16();
                        if (e) return e;
                        for (int gm5_kb = 0; gm5_kb < 80; gm5_kb++) {
                            e = hrt_hmx_mm_f16(V, 0 + (((size_t)(32) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm5_r * 80 + gm5_kb) * HRT_TILE_BYTES,
                                327680 + ((size_t)gm5_c * 80 + gm5_kb) * HRT_TILE_BYTES);
                            if (e) return e;
                        }
                        if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                    }
                    prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                    if (!(abl & 8)) {
                        hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 2949120) + (size_t)((size_t)(1) * (32 * 256)), 256, gm5_r, gm5_c);
                    }
                    prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                }
            }
            t0 = HAP_perf_get_qtimer_count();
            for (int gm6_r = 0; gm6_r < 1; gm6_r++) {
                for (int gm6_c = 0; gm6_c < 8; gm6_c++) {
                    if (!(abl & 4)) {
                        int e = hrt_acc_clear_f16();
                        if (e) return e;
                        for (int gm6_kb = 0; gm6_kb < 80; gm6_kb++) {
                            e = hrt_hmx_mm_f16(V, 0 + (((size_t)(32) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm6_r * 80 + gm6_kb) * HRT_TILE_BYTES,
                                327680 + (((size_t)(256) / 32) * 80 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm6_c * 80 + gm6_kb) * HRT_TILE_BYTES);
                            if (e) return e;
                        }
                        if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                    }
                    prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                    if (!(abl & 8)) {
                        hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 2981888) + (size_t)((size_t)(1) * (32 * 256)), 256, gm6_r, gm6_c);
                    }
                    prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                }
            }
            // T.Parallel(32) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool4_ctx_t ctx4 = { X, Wg, Wu, Wd, Slab, p, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool4_worker, &ctx4, 32);
            prof[9] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // T.Parallel(32) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool5_ctx_t ctx5 = { X, Wg, Wu, Wd, Slab, p, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool5_worker, &ctx5, 32);
            prof[10] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
        }
        for (int kp = 0; kp < 20; kp++) {
            // T.Parallel(4) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool6_ctx_t ctx6 = { X, Wg, Wu, Wd, Slab, kp, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool6_worker, &ctx6, 4);
            prof[11] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // T.Parallel(36) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool7_ctx_t ctx7 = { X, Wg, Wu, Wd, Slab, kp, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool7_worker, &ctx7, 36);
            prof[12] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // async_commit_group consumed by W-lane pool start
            // T.Parallel(36) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool8_ctx_t ctx8 = { X, Wg, Wu, Wd, Slab, kp, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool8_worker, &ctx8, 36);
            prof[13] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // async_commit_group consumed by W-lane pool start
            attnops_pool_wait_le_w(1);
            t0 = HAP_perf_get_qtimer_count();
            for (int gm9_r = 0; gm9_r < 1; gm9_r++) {
                for (int gm9_c = 0; gm9_c < 4; gm9_c++) {
                    if (!(abl & 4)) {
                        int e = hrt_acc_clear_f16();
                        if (e) return e;
                        for (int gm9_kb = 0; gm9_kb < 288; gm9_kb++) {
                            e = hrt_hmx_mm_f16(V, 3047424 + ((size_t)gm9_r * 288 + gm9_kb) * HRT_TILE_BYTES,
                                4227072 + ((size_t)gm9_c * 288 + gm9_kb) * HRT_TILE_BYTES);
                            if (e) return e;
                        }
                        if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                    }
                    prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                    if (!(abl & 8)) {
                        hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 6586368), 128, gm9_r, gm9_c);
                    }
                    prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                }
            }
            // T.Pipelined steady loop: first worker-pool phase is async; join before next pool phase or loop back-edge
            for (int rb2 = 0; rb2 < 30; rb2++) {
                // T.Parallel(36)+T.Parallel(32) -> fused Hexagon worker-pool phase (async start)
                attnops_tl_ffn_std_pool9_ctx_t ctx9 = { X, Wg, Wu, Wd, Slab, kp, rb2, abl, prof };
                tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
                attnops_pool_start_ctx(attnops_tl_ffn_std_pool9_worker, &ctx9, 68);
                // async_commit_group consumed by W-lane pool start
                attnops_pool_wait_le_w(1);
                t0 = HAP_perf_get_qtimer_count();
                for (int gm11_r = 0; gm11_r < 1; gm11_r++) {
                    for (int gm11_c = 0; gm11_c < 4; gm11_c++) {
                        if (!(abl & 4)) {
                            int e = hrt_acc_clear_f16();
                            if (e) return e;
                            for (int gm11_kb = 0; gm11_kb < 288; gm11_kb++) {
                                e = hrt_hmx_mm_f16(V, 3047424 + (((size_t)((((rb2 + 1) % 2) * 32)) / 32) * 288 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm11_r * 288 + gm11_kb) * HRT_TILE_BYTES,
                                    4227072 + ((size_t)gm11_c * 288 + gm11_kb) * HRT_TILE_BYTES);
                                if (e) return e;
                            }
                            if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                        }
                        prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                        t0 = HAP_perf_get_qtimer_count();
                        if (!(abl & 8)) {
                            hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 6586368) + (size_t)((size_t)(((rb2 + 1) % 2)) * (32 * 128)), 128, gm11_r, gm11_c);
                        }
                        prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                        t0 = HAP_perf_get_qtimer_count();
                    }
                }
                attnops_pool_join();
                prof[14] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            }
            attnops_pool_wait_le_w(0);
            t0 = HAP_perf_get_qtimer_count();
            for (int gm12_r = 0; gm12_r < 1; gm12_r++) {
                for (int gm12_c = 0; gm12_c < 4; gm12_c++) {
                    if (!(abl & 4)) {
                        int e = hrt_acc_clear_f16();
                        if (e) return e;
                        for (int gm12_kb = 0; gm12_kb < 288; gm12_kb++) {
                            e = hrt_hmx_mm_f16(V, 3047424 + (((size_t)(32) / 32) * 288 + ((size_t)(0) / 32)) * HRT_TILE_BYTES + ((size_t)gm12_r * 288 + gm12_kb) * HRT_TILE_BYTES,
                                4227072 + ((size_t)gm12_c * 288 + gm12_kb) * HRT_TILE_BYTES);
                            if (e) return e;
                        }
                        if (!(abl & 16)) { e = hrt_acc_read_f16(V, g_v.CFG, TL_ACC); if (e) return e; }
                    }
                    prof[2] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                    if (!(abl & 8)) {
                        hrt_tlgdn_acc_tile_to_vtcm_rm(V, TL_ACC, (f16 *)(V + 6586368) + (size_t)((size_t)(1) * (32 * 128)), 128, gm12_r, gm12_c);
                    }
                    prof[3] += (int32_t)(HAP_perf_get_qtimer_count() - t0);
                    t0 = HAP_perf_get_qtimer_count();
                }
            }
            // T.Parallel(32) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool10_ctx_t ctx10 = { X, Wg, Wu, Wd, Slab, kp, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool10_worker, &ctx10, 32);
            prof[15] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
            // T.Parallel(32) -> Hexagon worker-pool phase (sync join)
            attnops_tl_ffn_std_pool11_ctx_t ctx11 = { X, Wg, Wu, Wd, Slab, kp, abl, prof };
            tl_prof_phase_t0 = HAP_perf_get_qtimer_count();
            attnops_pool_run_ctx(attnops_tl_ffn_std_pool11_worker, &ctx11, 32);
            prof[16] += (int32_t)(HAP_perf_get_qtimer_count() - tl_prof_phase_t0);
        }
    }
    prof[4] = (int32_t)(HAP_perf_get_qtimer_count() - tt);
    return 0;
}
